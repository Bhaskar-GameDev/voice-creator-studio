# Render sets NODE_ENV=production, which makes `npm install` omit devDependencies.
# The build tooling (vite, @lovable.dev/vite-tanstack-config, plugins) lives in
# devDependencies, so force-include them. `include` overrides the NODE_ENV omit.
# This applies no matter what build command the host runs.
include=dev
